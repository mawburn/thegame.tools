/// <reference types="react" />
interface LogoProps {
    size: number;
    fullLogo?: boolean;
    alt?: string;
    priority?: boolean;
}
export declare const Logo: ({ size, fullLogo, alt, priority, }: LogoProps) => JSX.Element;
export {};
